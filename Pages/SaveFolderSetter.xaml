<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="UdpQuickShare.Pages.SaveFolderSetter"
             xmlns:model="clr-namespace:UdpQuickShare.ViewModels"
             Title="SaveFolderSetter">
<<<<<<< Updated upstream
    <VerticalStackLayout>
        <HorizontalStackLayout>
            <Label Text="使用直接路径(安卓速度更快)"/>
=======
    <Shell.TitleView>
        <HorizontalStackLayout x:Name="UseDirect">
            <Label Text="使用直接路径(安卓速度更快)"
                   VerticalTextAlignment="Center"/>
>>>>>>> Stashed changes
            <Switch x:Name="UseDirectPathSwitch"
                    Toggled="Switch_Toggled"/>
        </HorizontalStackLayout>
    </Shell.TitleView>
    <ContentPage.Resources>
        <DataTemplate x:Key="Saves" x:DataType="model:FolderItem">
            <StackLayout WidthRequest="300" HeightRequest="100">
                <StackLayout.GestureRecognizers>
                    <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped"/>
                </StackLayout.GestureRecognizers>
                <Label Text="{Binding Name}"
                       TextColor="Black"/>
                <Label Text="{Binding Path}"
                       TextColor="Green"/>
            </StackLayout>
        </DataTemplate>
    </ContentPage.Resources>
    <ScrollView>
        <FlexLayout Wrap="Wrap"
                x:Name="Folders"
                JustifyContent="SpaceEvenly"
                BindableLayout.ItemTemplate="{StaticResource Saves}" />
    </ScrollView>

</ContentPage>